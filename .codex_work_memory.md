# Codex Work Memory

## 2026-06-15 - Custom CSS UX and CSS Manager

- Improved the Settings > Layout > Custom Stylesheets area to show a table with stylesheet name, scope, custom toggle, selected custom file, status, and actions.
- Added `cssfile` form field to select files from `media/com_jem/css/custom/*.css`.
- Added responsive-specific custom CSS settings for backend, calendar, colorpicker, geostyle, googlemap, jem, and print stylesheets.
- Updated `JemHelper::loadCss()` to prefer responsive-specific custom files, with fallback to configured base custom CSS, custom default names, and then core CSS.
- Improved CSS Manager with Standard CSS and Custom CSS tables, status badges, active/missing usage detection, and a "Copy as custom" action.
- Removed the duplicated per-row Actions column from Settings > Layout > Custom Stylesheets; CSS editing now goes through the single CSS Manager button.
- Adjusted CSS Manager so the Custom table is always rendered and padded with empty rows when needed, keeping it visually aligned with the standard stylesheet table.
- Updated CSS Manager so standard CSS files are copied to custom with a user-defined target name, and custom rows align with the standard CSS row they override.
- Clarified Settings > Layout stylesheet scope by using Legacy/Responsive instead of General/Responsive for frontend layouts.
- Unified CSS Manager into one table: one row per standard CSS, with compatible custom variants listed inside the same row.
- Custom CSS copies now include `JEM custom source`, `JEM custom source version`, and `JEM custom source size` headers; relation metadata lives in the custom CSS file, not in SQL.
- Settings only lists custom files compatible with the CSS being configured, falling back to name-based matching for older custom files.
- Settings disables the custom file selector and the `Yes` option when no compatible custom CSS exists, while preserving configured missing values for correction.
- CSS Manager shows standard CSS size/current JEM version and custom CSS copy metadata so future updates can identify the original base of a custom stylesheet.
- Final CSS Manager layout separates standard CSS columns from custom CSS columns, removes the redundant Standard badge from Version, uses a single general Settings button above the table, and keeps custom Status and Edit actions in separate final columns.
- Updated backend CSS and English language strings for the new UX.
- Synced changes to:
  - `C:\GitHub\JEM-Project-4.5`
  - `C:\xampp\htdocs\jl542`
  - `C:\xampp\htdocs\jl602`
  - `C:\xampp\htdocs\elgurugu`
- Verified PHP syntax and `settings.xml` parsing successfully in all three local sites.
